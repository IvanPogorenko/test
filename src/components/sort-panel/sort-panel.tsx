import React from "react";
import {SortPanelProps} from "../../interfaces/car-sort.interface";

const SertPanel: React.FC<SortPanelProps> = ({sortBy, onSortChange}) => {
    return (
        
    );
};